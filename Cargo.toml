[package]
name = "git-cuttle"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
plotters ={ version = "^0.3.1", default_features = false, features = ["svg_backend"]}